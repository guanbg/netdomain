package com.platform.cubism.service;

public enum ServiceType {
	local, local_report, remote_http, remote_url, remote_auto, remote_socket, remote_jms, webService, customService, httpService, unknown
}